'use client';
import ReactQueryProvider from './ReactQueryProvider';

export default function Home() {
  return (
    <ReactQueryProvider>
      <section>hello</section>
    </ReactQueryProvider>
  );
}
